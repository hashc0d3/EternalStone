import { SiteShell } from '@/components/site-shell';

export default function AboutPage() {
  return (
    <SiteShell>
      <h1 className="text-3xl font-semibold">О компании</h1>
      <p className="mt-3 text-[var(--muted)]">Раздел в работе.</p>
    </SiteShell>
  );
}
